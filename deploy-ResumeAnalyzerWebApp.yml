name: Deploy Static Web App

on:
  push:
    branches:
      - master

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  RESOURCE_GROUP: ResumeAnalyzer
  STATIC_WEB_APP_NAME: resumeanalyzer

jobs:
  deploy:
    runs-on: windows-latest

    environment:
      name: Production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Blazor App Artifact
        uses: actions/download-artifact@v4
        with:
          name: blazor-app
          path: publish

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_SALMON_PEBBLE_078B96F03 }}
          action: upload
          app_location: "publish"
          skip_app_build: true